import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  formWidth: {
    width: "100%",
  },
  formContainer: {
    marginBottom: "1.5rem",
  },
  formTitle: {
    fontSize: "1.2rem",
    fontWeight: 500,
  },
  formButtonContainer: {
    display: "flex",
    flexDirection: "row-reverse",
  },
}));
