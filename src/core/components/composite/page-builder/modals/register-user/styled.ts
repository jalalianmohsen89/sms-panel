import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  width: {
    width: "100%",
  },
  formContainer: {
    marginBottom: "1.5rem",
  },
  title: {
    fontSize: "1.2rem",
    fontWeight: 500,
  },
}));
