import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  container: {
    width: "100%",
    height: "80vh",
  },
  imageContainer: {
    marginBottom: "3rem",
  },
  titleContainer: {
    fontSize: "1.5rem",
    fontWeight: 500,
    color: "#333",
  },
  button: {
    marginTop: "2rem",
  },
}));
